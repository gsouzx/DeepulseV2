PORT=3001

# Comma-separated list of origins allowed to connect (REST CORS is unchanged
# for now — this only gates the new Socket.io realtime layer).
ALLOWED_ORIGINS=http://localhost:5500,http://127.0.0.1:5500
